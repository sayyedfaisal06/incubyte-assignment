const add = (nums) => {
  if (nums === "") return 0;

  return 0;
};

const runTest = () => {
  // Test 1: Empty string returns 0
  if (add("") === 0) {
    console.log("Test 1 Failed: Expected 0 for empty string");
  }
};

runTest();
